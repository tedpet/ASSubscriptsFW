package com.as.model;

import org.apache.log4j.Logger;

public class Permissions extends com.as.model.gen._Permissions {
	@SuppressWarnings("unused")
	private static Logger log = Logger.getLogger(Permissions.class);
}
