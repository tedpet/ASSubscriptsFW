package com.as.model;

import org.apache.log4j.Logger;

public class Subscription extends com.as.model.gen._Subscription {
	@SuppressWarnings("unused")
	private static Logger log = Logger.getLogger(Subscription.class);
}
